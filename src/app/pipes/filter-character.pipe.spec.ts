import { FilterCharacterPipe } from './filter-character.pipe';

describe('FilterCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
